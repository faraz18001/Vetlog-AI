import os
from langchain_core.tools import tool
from langchain_ollama import ChatOllama
from langchain_openai import ChatOpenAI
from langraph.checkpoint.memory import MemorySaver
from langgraph.prebuilt import create_react_agent


def get_llm_model():
    provider = os.getenv("AGENT_PROVIDER", "ollama").lower()
    base_url = os.getenv("OLLAMA_BASE_URL", "https://ollama.com")
    model_name = os.getenv("OLLAMA_MODEL", "gpt-oss:20b-cloud")
    api_key = os.getenv("OLLAMA_API_KEY", "")

    if provider == "ollama":
        if "v1" in base_url:
            return ChatOpenAI(
                base_url=base_url,
                api_key=api_key or "ollama",
                model_name=model_name,
                temperature=0.2,
            )

    else:
        open_ai_key = os.getenv("OPENAI_API_KEY", "")
        return ChatOpenAI(
            api_key=open_ai_key,
            model=os.getenv("DEFAULT_MODEL", "gpt40"),
            temperature=0.2,
        )


def initialize_agent():
    chat_model = get_llm_model()
    # tools are needed to be called here(sql tool needs to be called here )

    agent_graph = create_react_agent(
        model=chat_model,
        tools=tools,
        check_pointer=agent_check_pointer,
        prompt=SYSTEM_PRPOMP,
    )
